# Wifi-Driver-hp-kali-linux
## installation 

```
sudo apt-get update
git clone https://github.com/shubhamchauda/Wifi-Driver-hp-kali
cd BroadcomInstaller2017
sudo chmod +x Broadcom.sh
sudo bash Broadcom.sh
```
